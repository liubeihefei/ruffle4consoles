diff --git a/core/src/avm2/bytearray.rs b/core/src/avm2/bytearray.rs
index 3df385b61..4a5452cc4 100644
--- a/core/src/avm2/bytearray.rs
+++ b/core/src/avm2/bytearray.rs
@@ -11,6 +11,7 @@ use std::cmp;
 use std::fmt::{self, Display, Formatter};
 use std::io::prelude::*;
 use std::io::{self, SeekFrom};
+use std::sync::Arc;
 
 #[derive(Clone, Collect, Debug, Copy, PartialEq, Eq)]
 #[collect(no_drop)]
@@ -86,7 +87,7 @@ pub enum ObjectEncoding {
 #[derive(Clone, Debug)]
 pub struct ByteArrayStorage {
     /// Underlying ByteArray
-    bytes: Vec<u8>,
+    bytes: Arc<Vec<u8>>,
 
     /// The current position to read/write from
     position: Cell<usize>,
@@ -102,7 +103,7 @@ impl ByteArrayStorage {
     /// Create a new ByteArrayStorage
     pub fn new(context: &mut UpdateContext<'_>) -> ByteArrayStorage {
         ByteArrayStorage {
-            bytes: Vec::new(),
+            bytes: Arc::new(Vec::new()),
             position: Cell::new(0),
             endian: Endian::Big,
             object_encoding: context.avm2.default_bytearray_encoding,
@@ -112,7 +113,7 @@ impl ByteArrayStorage {
     /// Create a new ByteArrayStorage using an already existing vector
     pub fn from_vec(context: &mut UpdateContext<'_>, bytes: Vec<u8>) -> ByteArrayStorage {
         ByteArrayStorage {
-            bytes,
+            bytes: Arc::new(bytes),
             position: Cell::new(0),
             endian: Endian::Big,
             object_encoding: context.avm2.default_bytearray_encoding,
@@ -177,7 +178,7 @@ impl ByteArrayStorage {
         if self.len() < new_len {
             self.set_length(new_len);
         }
-        self.bytes
+        Arc::make_mut(&mut self.bytes)
             .get_mut(offset..new_len)
             .expect("ByteArray write out of bounds")
             .copy_from_slice(buf);
@@ -187,7 +188,7 @@ impl ByteArrayStorage {
     /// Write bytes at any offset in the ByteArray
     /// Will return an error if the new buffer does not fit the ByteArray
     pub fn write_at_nongrowing(&mut self, buf: &[u8], offset: usize) -> Result<(), ByteArrayError> {
-        self.bytes
+        Arc::make_mut(&mut self.bytes)
             .get_mut(offset..)
             .and_then(|bytes| bytes.get_mut(..buf.len()))
             .ok_or(ByteArrayError::IndexOutOfBounds)?
@@ -220,7 +221,7 @@ impl ByteArrayStorage {
             self.set_length(new_len);
         }
 
-        self.bytes.copy_within(start..end, offset);
+        Arc::make_mut(&mut self.bytes).copy_within(start..end, offset);
         Ok(())
     }
 
@@ -231,17 +232,19 @@ impl ByteArrayStorage {
             CompressionAlgorithm::Zlib => {
                 // Note: some content is sensitive to compression type
                 // (as it's visible in the header)
-                let mut encoder = ZlibEncoder::new(&*self.bytes, Compression::best());
+                let mut encoder = ZlibEncoder::new(self.bytes.as_slice(), Compression::best());
                 encoder.read_to_end(&mut buffer).err().map(|e| e.into())
             }
             CompressionAlgorithm::Deflate => {
-                let mut encoder = DeflateEncoder::new(&*self.bytes, Compression::best());
+                let mut encoder = DeflateEncoder::new(self.bytes.as_slice(), Compression::best());
                 encoder.read_to_end(&mut buffer).err().map(|e| e.into())
             }
             #[cfg(feature = "lzma")]
-            CompressionAlgorithm::Lzma => lzma_rs::lzma_compress(&mut &*self.bytes, &mut buffer)
-                .err()
-                .map(|e| e.into()),
+            CompressionAlgorithm::Lzma => {
+                lzma_rs::lzma_compress(&mut self.bytes.as_slice(), &mut buffer)
+                    .err()
+                    .map(|e| e.into())
+            }
             #[cfg(not(feature = "lzma"))]
             CompressionAlgorithm::Lzma => Some("Ruffle was not compiled with LZMA support".into()),
         };
@@ -258,17 +261,19 @@ impl ByteArrayStorage {
         let mut buffer = Vec::new();
         let error: Option<Box<dyn std::error::Error>> = match algorithm {
             CompressionAlgorithm::Zlib => {
-                let mut decoder = ZlibDecoder::new(&*self.bytes);
+                let mut decoder = ZlibDecoder::new(self.bytes.as_slice());
                 decoder.read_to_end(&mut buffer).err().map(|e| e.into())
             }
             CompressionAlgorithm::Deflate => {
-                let mut decoder = DeflateDecoder::new(&*self.bytes);
+                let mut decoder = DeflateDecoder::new(self.bytes.as_slice());
                 decoder.read_to_end(&mut buffer).err().map(|e| e.into())
             }
             #[cfg(feature = "lzma")]
-            CompressionAlgorithm::Lzma => lzma_rs::lzma_decompress(&mut &*self.bytes, &mut buffer)
-                .err()
-                .map(|e| e.into()),
+            CompressionAlgorithm::Lzma => {
+                lzma_rs::lzma_decompress(&mut self.bytes.as_slice(), &mut buffer)
+                    .err()
+                    .map(|e| e.into())
+            }
             #[cfg(not(feature = "lzma"))]
             CompressionAlgorithm::Lzma => Some("Ruffle was not compiled with LZMA support".into()),
         };
@@ -306,18 +311,18 @@ impl ByteArrayStorage {
 
     #[inline]
     pub fn clear(&mut self) {
-        self.bytes.clear();
+        Arc::make_mut(&mut self.bytes).clear();
         self.position.set(0)
     }
 
     #[inline]
     pub fn shrink_to_fit(&mut self) {
-        self.bytes.shrink_to_fit()
+        Arc::make_mut(&mut self.bytes).shrink_to_fit()
     }
 
     #[inline]
     pub fn set_length(&mut self, new_len: usize) {
-        self.bytes.resize(new_len, 0);
+        Arc::make_mut(&mut self.bytes).resize(new_len, 0);
         self.position.set(self.position().min(new_len));
     }
 
@@ -327,19 +332,19 @@ impl ByteArrayStorage {
 
     pub fn set(&mut self, item: usize, value: u8) {
         if self.len() < (item + 1) {
-            self.bytes.resize(item + 1, 0)
+            Arc::make_mut(&mut self.bytes).resize(item + 1, 0)
         }
 
-        *self.bytes.get_mut(item).unwrap() = value;
+        *Arc::make_mut(&mut self.bytes).get_mut(item).unwrap() = value;
     }
 
     /// Write a single byte at any offset in the bytearray, panicking if out of bounds.
     pub fn set_nongrowing(&mut self, item: usize, value: u8) {
-        self.bytes[item] = value;
+        Arc::make_mut(&mut self.bytes)[item] = value;
     }
 
     pub fn delete(&mut self, item: usize) {
-        if let Some(i) = self.bytes.get_mut(item) {
+        if let Some(i) = Arc::make_mut(&mut self.bytes).get_mut(item) {
             *i = 0;
         }
     }
@@ -351,7 +356,12 @@ impl ByteArrayStorage {
 
     #[inline]
     pub fn bytes_mut(&mut self) -> &mut [u8] {
-        &mut self.bytes
+        Arc::make_mut(&mut self.bytes).as_mut_slice()
+    }
+
+    #[inline]
+    pub fn shared_bytes(&self) -> Arc<Vec<u8>> {
+        self.bytes.clone()
     }
 
     #[inline]
@@ -488,3 +498,25 @@ macro_rules! impl_read{
 
 impl_write!(write_float f32, write_double f64, write_int i32, write_unsigned_int u32, write_short i16, write_unsigned_short u16);
 impl_read!(read_float read_float_at 4; f32, read_double read_double_at 8; f64, read_int read_int_at 4; i32, read_unsigned_int read_unsigned_int_at 4; u32, read_short read_short_at 2; i16, read_unsigned_short read_unsigned_short_at 2; u16, read_byte read_byte_at 1; i8, read_unsigned_byte read_unsigned_byte_at 1; u8);
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn shared_bytes_are_copy_on_write() {
+        let mut storage = ByteArrayStorage {
+            bytes: Arc::new(vec![1, 2, 3]),
+            position: Cell::new(0),
+            endian: Endian::Big,
+            object_encoding: ObjectEncoding::Amf3,
+        };
+        let shared = storage.shared_bytes();
+
+        storage.set_nongrowing(1, 9);
+
+        assert_eq!(shared.as_slice(), &[1, 2, 3]);
+        assert_eq!(ByteArrayStorage::bytes(&storage), &[1, 9, 3]);
+        assert!(!Arc::ptr_eq(&shared, &storage.bytes));
+    }
+}
diff --git a/core/src/avm2/globals/flash/display/loader.rs b/core/src/avm2/globals/flash/display/loader.rs
index d2e4574f6..ecf0ca09d 100644
--- a/core/src/avm2/globals/flash/display/loader.rs
+++ b/core/src/avm2/globals/flash/display/loader.rs
@@ -235,7 +235,12 @@ pub fn load_bytes<'gc>(
     let this = this.as_object().unwrap();
 
     let arg0 = args.get_object(activation, 0, "data")?;
-    let bytes = arg0.as_bytearray().unwrap().bytes().to_vec();
+    let bytes = arg0.as_bytearray().unwrap().shared_bytes();
+    tracing::info!(
+        target: "ruffle_core::loader",
+        bytes_shared = bytes.len(),
+        "Reusing ByteArray storage for Loader.loadBytes"
+    );
     let context = args.try_get_object(1);
 
     let loader_info = this
diff --git a/core/src/character.rs b/core/src/character.rs
index d5e7c23d1..cfa2bde44 100644
--- a/core/src/character.rs
+++ b/core/src/character.rs
@@ -1,3 +1,4 @@
+use std::borrow::Cow;
 use std::cell::OnceCell;
 
 use crate::backend::audio::SoundHandle;
@@ -6,13 +7,14 @@ use crate::display_object::{
     Avm1Button, Avm2Button, BitmapClass, EditText, Graphic, MorphShape, MovieClip, Text, Video,
 };
 use crate::font::Font;
+use crate::tag_utils::SwfSlice;
 use gc_arena::barrier::unlock;
 use gc_arena::lock::Lock;
 use gc_arena::{Collect, Gc, Mutation};
 use ruffle_render::backend::RenderBackend;
 use ruffle_render::bitmap::{Bitmap as RenderBitmap, BitmapHandle, BitmapSize};
 use ruffle_render::error::Error as RenderError;
-use swf::DefineBitsLossless;
+use swf::{BitmapFormat, CharacterId, DefineBitsLossless};
 
 #[derive(Copy, Clone, Collect, Debug)]
 #[collect(no_drop)]
@@ -88,12 +90,36 @@ impl<'gc> BitmapCharacter<'gc> {
 #[derive(Clone, Debug)]
 pub enum CompressedBitmap {
     Jpeg {
-        data: Vec<u8>,
-        alpha: Option<Vec<u8>>,
+        data: CompressedBitmapData,
+        alpha: Option<SwfSlice>,
         width: u16,
         height: u16,
     },
-    Lossless(DefineBitsLossless<'static>),
+    Lossless {
+        data: SwfSlice,
+        id: CharacterId,
+        format: BitmapFormat,
+        width: u16,
+        height: u16,
+        version: u8,
+    },
+}
+
+/// Compressed bitmap bytes either produced during parsing or borrowed from the
+/// decompressed SWF tag stream.
+#[derive(Clone, Debug)]
+pub enum CompressedBitmapData {
+    Owned(Vec<u8>),
+    Swf(SwfSlice),
+}
+
+impl AsRef<[u8]> for CompressedBitmapData {
+    fn as_ref(&self) -> &[u8] {
+        match self {
+            Self::Owned(data) => data,
+            Self::Swf(data) => data.data(),
+        }
+    }
 }
 
 impl CompressedBitmap {
@@ -103,9 +129,9 @@ impl CompressedBitmap {
                 width: *width,
                 height: *height,
             },
-            CompressedBitmap::Lossless(define_bits_lossless) => BitmapSize {
-                width: define_bits_lossless.width,
-                height: define_bits_lossless.height,
+            CompressedBitmap::Lossless { width, height, .. } => BitmapSize {
+                width: *width,
+                height: *height,
             },
         }
     }
@@ -116,9 +142,27 @@ impl CompressedBitmap {
                 alpha,
                 width: _,
                 height: _,
-            } => ruffle_render::utils::decode_define_bits_jpeg(data, alpha.as_deref()),
-            CompressedBitmap::Lossless(define_bits_lossless) => {
-                ruffle_render::utils::decode_define_bits_lossless(define_bits_lossless)
+            } => ruffle_render::utils::decode_define_bits_jpeg(
+                data.as_ref(),
+                alpha.as_ref().map(SwfSlice::data),
+            ),
+            CompressedBitmap::Lossless {
+                data,
+                id,
+                format,
+                width,
+                height,
+                version,
+            } => {
+                let define_bits_lossless = DefineBitsLossless {
+                    data: Cow::Borrowed(data.data()),
+                    id: *id,
+                    format: *format,
+                    width: *width,
+                    height: *height,
+                    version: *version,
+                };
+                ruffle_render::utils::decode_define_bits_lossless(&define_bits_lossless)
             }
         }
     }
diff --git a/core/src/display_object/movie_clip.rs b/core/src/display_object/movie_clip.rs
index 767b1bb1c..1465747fb 100644
--- a/core/src/display_object/movie_clip.rs
+++ b/core/src/display_object/movie_clip.rs
@@ -14,7 +14,7 @@ use crate::backend::audio::{AudioManager, SoundInstanceHandle};
 use crate::backend::navigator::Request;
 use crate::backend::ui::MouseCursor;
 use crate::binary_data::BinaryData;
-use crate::character::{BitmapCharacter, Character, CompressedBitmap};
+use crate::character::{BitmapCharacter, Character, CompressedBitmap, CompressedBitmapData};
 use crate::context::{ActionType, RenderContext, UpdateContext};
 use crate::display_object::container::{dispatch_removed_event, ChildContainer};
 use crate::display_object::interactive::{
@@ -52,7 +52,7 @@ use std::cmp::max;
 use std::collections::HashMap;
 use std::sync::Arc;
 use swf::extensions::ReadSwfExt;
-use swf::{ClipEventFlag, DefineBitsLossless, FrameLabelData, TagCode, UTF_8};
+use swf::{ClipEventFlag, FrameLabelData, TagCode, UTF_8};
 
 use super::interactive::Avm2MousePick;
 use super::BitmapClass;
@@ -573,6 +573,17 @@ impl<'gc> MovieClip<'gc> {
         shared.import_exports_of_importer(context);
 
         if is_finished {
+            let bitmap_bytes_reused = progress.bitmap_bytes_reused.get();
+            if bitmap_bytes_reused != 0 {
+                tracing::info!(
+                    target: "ruffle_core::bitmap",
+                    movie_url = swf.movie.url(),
+                    clip_id = shared.id,
+                    bitmap_slices_reused = progress.bitmap_slices_reused.get(),
+                    bitmap_bytes_reused,
+                    "Reused compressed bitmap data from the SWF tag stream"
+                );
+            }
             if progress.cur_preload_frame.get() == 1 {
                 // If this clip did not have any show frame tags,
                 // treat the end-of-clip as a ShowFrame
@@ -3293,16 +3304,18 @@ impl<'gc, 'a> MovieClipShared<'gc> {
         version: u8,
     ) -> Result<(), Error> {
         let define_bits_lossless = reader.read_define_bits_lossless(version)?;
+        let data = self.swf.to_subslice(define_bits_lossless.data.as_ref());
+        self.record_reused_bitmap_data(data.len());
         let bitmap = Gc::new(
             context.gc(),
-            BitmapCharacter::new(CompressedBitmap::Lossless(DefineBitsLossless {
+            BitmapCharacter::new(CompressedBitmap::Lossless {
                 id: define_bits_lossless.id,
                 format: define_bits_lossless.format,
                 width: define_bits_lossless.width,
                 height: define_bits_lossless.height,
                 version: define_bits_lossless.version,
-                data: Cow::Owned(define_bits_lossless.data.into_owned()),
-            })),
+                data,
+            }),
         );
         self.library_mut(context)
             .register_character(define_bits_lossless.id, Character::Bitmap(bitmap));
@@ -3420,9 +3433,17 @@ impl<'gc, 'a> MovieClipShared<'gc> {
         let library = self.library_mut(context);
         let (id, jpeg_data) = reader.read_define_bits()?;
         let jpeg_tables = library.jpeg_tables();
-        let jpeg_data =
-            ruffle_render::utils::glue_tables_to_jpeg(jpeg_data, jpeg_tables).into_owned();
-        let (width, height) = ruffle_render::utils::decode_define_bits_jpeg_dimensions(&jpeg_data)?;
+        let jpeg_data = ruffle_render::utils::glue_tables_to_jpeg(jpeg_data, jpeg_tables);
+        let (width, height) =
+            ruffle_render::utils::decode_define_bits_jpeg_dimensions(jpeg_data.as_ref())?;
+        let jpeg_data = match jpeg_data {
+            Cow::Borrowed(jpeg_data) => {
+                let jpeg_data = self.swf.to_subslice(jpeg_data);
+                self.record_reused_bitmap_data(jpeg_data.len());
+                CompressedBitmapData::Swf(jpeg_data)
+            }
+            Cow::Owned(jpeg_data) => CompressedBitmapData::Owned(jpeg_data),
+        };
         let bitmap = Character::Bitmap(Gc::new(
             mc,
             BitmapCharacter::new(CompressedBitmap::Jpeg {
@@ -3444,10 +3465,12 @@ impl<'gc, 'a> MovieClipShared<'gc> {
     ) -> Result<(), Error> {
         let (id, jpeg_data) = reader.read_define_bits_jpeg_2()?;
         let (width, height) = ruffle_render::utils::decode_define_bits_jpeg_dimensions(jpeg_data)?;
+        let jpeg_data = self.swf.to_subslice(jpeg_data);
+        self.record_reused_bitmap_data(jpeg_data.len());
         let bitmap = Character::Bitmap(Gc::new(
             context.gc(),
             BitmapCharacter::new(CompressedBitmap::Jpeg {
-                data: jpeg_data.to_owned(),
+                data: CompressedBitmapData::Swf(jpeg_data),
                 alpha: None,
                 width,
                 height,
@@ -3466,12 +3489,16 @@ impl<'gc, 'a> MovieClipShared<'gc> {
     ) -> Result<(), Error> {
         let jpeg = reader.read_define_bits_jpeg_3(version)?;
         let (width, height) = ruffle_render::utils::decode_define_bits_jpeg_dimensions(jpeg.data)?;
+        let jpeg_data = self.swf.to_subslice(jpeg.data);
+        let alpha_data = self.swf.to_subslice(jpeg.alpha_data);
+        self.record_reused_bitmap_data(jpeg_data.len());
+        self.record_reused_bitmap_data(alpha_data.len());
 
         let bitmap = Character::Bitmap(Gc::new(
             context.gc(),
             BitmapCharacter::new(CompressedBitmap::Jpeg {
-                data: jpeg.data.to_owned(),
-                alpha: Some(jpeg.alpha_data.to_owned()),
+                data: CompressedBitmapData::Swf(jpeg_data),
+                alpha: Some(alpha_data),
                 width,
                 height,
             }),
@@ -4448,6 +4475,12 @@ struct PreloadProgress {
     /// If this movie is currently executing an ImportAssets/2.
     /// If true, this movie should **not** execute, and should be considered as still loading.
     awaiting_import: Cell<bool>,
+
+    /// Number of compressed bitmap slices retained by reference instead of copied.
+    bitmap_slices_reused: Cell<usize>,
+
+    /// Number of compressed bitmap bytes retained by reference instead of copied.
+    bitmap_bytes_reused: Cell<usize>,
 }
 
 impl Default for PreloadProgress {
@@ -4459,6 +4492,8 @@ impl Default for PreloadProgress {
             start_pos: Cell::new(0),
             cur_preload_symbol: Cell::new(None),
             awaiting_import: Cell::new(false),
+            bitmap_slices_reused: Cell::new(0),
+            bitmap_bytes_reused: Cell::new(0),
         }
     }
 }
@@ -4516,6 +4551,21 @@ struct EagerTags {
 }
 
 impl<'gc> MovieClipShared<'gc> {
+    fn record_reused_bitmap_data(&self, bytes: usize) {
+        self.preload_progress.bitmap_slices_reused.set(
+            self.preload_progress
+                .bitmap_slices_reused
+                .get()
+                .saturating_add(1),
+        );
+        self.preload_progress.bitmap_bytes_reused.set(
+            self.preload_progress
+                .bitmap_bytes_reused
+                .get()
+                .saturating_add(bytes),
+        );
+    }
+
     fn empty(movie: Arc<SwfMovie>) -> Self {
         let mut s = Self::with_data(0, SwfSlice::empty(movie), 1, None, None);
 
diff --git a/core/src/loader.rs b/core/src/loader.rs
index e8731abf5..a285046f1 100644
--- a/core/src/loader.rs
+++ b/core/src/loader.rs
@@ -375,7 +375,7 @@ impl<'gc> LoadManager<'gc> {
     pub fn load_movie_into_clip_bytes(
         context: &mut UpdateContext<'gc>,
         target_clip: DisplayObject<'gc>,
-        bytes: Vec<u8>,
+        bytes: Arc<Vec<u8>>,
         vm_data: MovieLoaderVMData<'gc>,
     ) -> Result<(), Error> {
         let loader = MovieLoader {
@@ -779,7 +779,7 @@ impl<'gc> MovieLoader<'gc> {
     pub fn movie_loader_bytes(
         handle: LoaderHandle,
         uc: &mut UpdateContext<'gc>,
-        bytes: Vec<u8>,
+        bytes: Arc<Vec<u8>>,
     ) -> Result<(), Error> {
         let clip = match uc.load_manager.get_loader(handle) {
             Some(Self { target_clip, .. }) => *target_clip,
